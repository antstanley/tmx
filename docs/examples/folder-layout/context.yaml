# Standalone context.* — inherited by every task in this folder.
kind: context
name: default
env:
  LOG_LEVEL: info
secrets:
  API_KEY:
    env: API_KEY
hooks:
  create:
    - name: init
      type: execute
      with:
        command: mkdir -p .work
