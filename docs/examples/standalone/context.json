{
  "kind": "context",
  "name": "default",
  "env": { "LOG_LEVEL": "info" },
  "secrets": {
    "API_KEY": { "env": "API_KEY" }
  },
  "hooks": {
    "create": [
      { "name": "init", "type": "exec", "with": { "command": "mkdir -p .work" } }
    ]
  }
}
