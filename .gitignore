# Local validation venv (created by scripts/validate.sh when uv is unavailable)
.venv-tmx/

# Python caches
__pycache__/
*.pyc

# Rust build artifacts
/target
**/target/

# Local run store — the RunStore persists each run here (record, not a journal)
**/.tmx/runs/
