[package]
name = "tmx-schema"
description = "TMX input data model (Flow/Task/Context/Environment) and the runtime limit constants."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true

# No dependencies yet: the deserialise types (serde/serde_json/indexmap) land with tasks 02–03.
# This crate is the bottom of the graph and depends on nothing outward.

[lints]
workspace = true
