[package]
name = "tmx-schema"
description = "TMX input data model (Flow/Task/Context/Environment) and the runtime limit constants."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true

# Task 02 adds `serde` for the `MatcherName` derive; `serde_json` (dev-only) drives the matcher
# round-trip test. `serde_json`/`indexmap` become normal deps with the deserialise types in task 03.
# This crate is the bottom of the graph and depends on nothing outward — all pure data, no I/O.
[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
