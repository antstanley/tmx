# Done Certificate — Task 15: Preflight (load → resolve → validate → capability)

**Task:** [15-preflight.md](15-preflight.md) · **Plan:** [plan.md](../plan.md)
**State:** Validated 2026-07-06

> This certificate is a verification protocol for Task 15. A validating agent discharges it:
> for each obligation, collect the named evidence, run the named checks, set the Status, then
> derive the Conclusion by the rubric below. Do not mark an obligation SATISFIED without its
> evidence; do not record DONE with any non-SATISFIED obligation.

## Definition

DONE(Task 15) ≡ every obligation O1…O4 below holds, each backed by the evidence the obligation
names (a file location, a test result, or an execution trace) — not by assertion.

## Premises

- **P1 — Goal.** Deliver the `tmx-core` preflight orchestration that either passes wholesale to a `ResolvedFlow` + `CapabilitySet` or fails fast with nothing executed.
- **P2 — Obligations.** Done iff O1…O4 all hold; O4 is the Reviewable item.
- **P3 — Invariants.** The Task-13 loader/resolver and Task-14 validator this composes must keep passing their tests; a valid corpus Flow that preflighted before still preflights; `tmx-core` stays pure (Task-01 purity gate).

## Obligations

- **O1 — A passing preflight yields a `ResolvedFlow` (ordered `Vec<Task>`, resolved env/context) and a `CapabilitySet`; a directory with one malformed task aborts before any task executes.**
  - *Claim:* A valid Flow (single-file or directory) preflights to a `ResolvedFlow` with an ordered `Vec<Task>` and resolved env/context plus a `CapabilitySet`; a directory containing one malformed task aborts in preflight with nothing executed.
  - *Evidence to collect:* Read `crates/tmx-core/src/preflight.rs`, the desugar helpers in `crates/tmx-schema/src/`, and `ValidateArtifacts`/`InspectFlow` in `crates/tmx-core/src/usecases.rs`. Preflight `docs/examples/folder-layout/` (`context.yaml`, `environment.toml`, `task-1.jsonc`, `task-2.yaml`) and expect a `ResolvedFlow` + `CapabilitySet`. Run the malformed-task-in-a-directory test and expect an abort before any task runs.
  - *Checks:* Trace a directory of task files through natural-filename ordering and confirm `task-2` precedes `task-10` (maximal digit runs compared as unsigned integers, byte-wise ASCII, case-sensitive); confirm the malformed-task abort happens in preflight, before the runner is invoked (nothing executed).
  - *Status:* ☒ SATISFIED — the inline and directory forms fully hold: `docs/examples/folder-layout/` preflights over the REAL adapters to a `ResolvedFlow` with `task-1` (fetch-config) before `task-2` (write-report) and shared env/context folded in (`tmx-adapters::preflight::folder_layout_assembles_in_natural_order_with_shared_env_and_context` PASS); a constructed `task-1`/`task-2`/`task-10` directory ordered `one, two, ten` end-to-end (validator probe, PASS); the malformed-task directory aborts in preflight with `Validation`/`schema_invalid`, nothing executed (core + adapters tests PASS); preflight never invokes a runner (no runner call in `preflight.rs`). The previously-UNSATISFIED reference form is now closed: `resolve_references` (preflight.rs, wired between `assemble_flow` and `resolve`) chases `EnvironmentRef::Reference`/`ContextRef::Reference` and every `Hook::Reference`/`Hook::Use` body through the `ReferenceResolver`/`SourceLoader` ports, kind-dispatches + depth-bounds + schema-validates each target, and inlines it before `resolve()` runs. Verified by tests AND independent validator probes over the REAL adapters: a flow with `environment: "./environment.toml"` + `context: "./context.yaml"` string refs preflights and the inlined targets' capabilities (Provider/Secret) land in the set (core test `a_referenced_environment_and_context_are_chased_and_inlined` + probe_2 PASS); a dangling env reference is typed `Resolution`/`reference_not_found` (test + PASS); a kind-contradicting target (`environment:` → a `kind: context` file) is `Resolution`/`reference_kind_mismatch` (probe_3 PASS); the corpus `single-file-flow.yaml`'s `error: ./hooks/on-error.yaml` hook reference is genuinely chased — dangling in-corpus it now fails typed `reference_not_found` (probe_1), and with the target staged it preflights end-to-end (`single_file_flow_still_preflights_after_the_loader_and_validator_compose` PASS). Also noted: `ValidateArtifacts`/`InspectFlow` do not exist in `usecases.rs` (Pointer only, deferred to tasks 28/31 — accepted, not load-bearing for the claim).

- **O2 — Over-limit counts/widths/depths and a nameless or duplicate-named task are rejected at preflight, and a Flow needing an unwired capability returns `missing_capability` naming the port and task type.**
  - *Claim:* The preflightable limits (`too_many_tasks`, literal `fanout_too_wide`, `json_too_deep`, `concurrency_too_high`, `too_many_hook_tasks`) and the structural checks (`missing_task_name`, duplicate-name `ResolutionError`) reject at preflight; a Flow requiring an absent or denying-stub adapter returns `EnvironmentError`/`missing_capability` naming the port and task type.
  - *Evidence to collect:* Read the limit and capability computation in `crates/tmx-core/src/preflight.rs`. Run the over-limit tests (counts / widths / depths) and the nameless and duplicate-name tests and expect `Validation` / `ResolutionError`. Run the missing-capability test and expect `missing_capability` naming the port and task type.
  - *Checks:* Trace the capability computation recursing into `map`/`eval` inner tasks, `eval` scorer kinds, hook bodies, and provider methods, so a required-but-unwired adapter surfaces as `missing_capability` naming the port and task type.
  - *Status:* ☒ SATISFIED — all five limit codes verified tripping as `Validation`: `too_many_tasks` (TASKS_PER_FLOW_MAX+1), `concurrency_too_high` (CONCURRENCY_MAX+1), `fanout_too_wide` (FANOUT_WIDTH_MAX+1 literal items), `json_too_deep` (200-deep array) in `tmx-core::preflight::over_limit_counts_widths_depths_and_concurrency_are_rejected` (PASS), and `too_many_hook_tasks` (HOOK_TASKS_MAX+1 create-hook tasks) now covered by the suite itself (`too_many_hook_tasks_across_a_context_is_rejected` PASS) and by a validator probe proving a *referenced* hook body's inlined tasks count against the budget (probe_4: HOOK_TASKS_MAX+1 tasks behind a `create: ./big-hook.yaml` reference → `Validation`/`too_many_hook_tasks`, PASS). `missing_task_name` is `Validation` and duplicate names are `Resolution`/`duplicate_task_name` (`nameless_task_is_validation_and_duplicate_name_is_resolution` PASS). `missing_capability` is `Environment`, names the port in the message (`ObjectStore`, `HttpClient`) and the task type in `err.task` (`store`, `fetch`) — verified over fakes and over the real adapters with `AvailableCapabilities::all().without(...)` modelling the denying stub. Capability walk traced: map/eval inner tasks, eval scorer kinds (`llmRubric`→Chat, `exec`/`run`→Process), inline hook bodies, structured secrets→Secret, provider→Provider. Referenced hook bodies now contribute both budget and capabilities: `resolve_references` inlines them to `Hook::Tasks` upstream, and an unwired ProcessRunner behind a `create` hook that references an exec Flow trips `missing_capability` (`a_referenced_hook_body_pulls_its_ports_into_the_capability_set` PASS — the prior O2 residue is closed). Remaining residue: provider *method bodies* are not recursed (needs the task-25 provider registry to load a manifest; gating on `EnvironmentProvider` presence is the task-15-scope realization).

- **O3 — Meets the repo definition of done.**
  - *Claim:* tests pass, clippy and rustfmt clean, every new bound is a named units-last constant, and `tmx-core` stays pure.
  - *Evidence to collect:* Run `cargo nextest run`, `cargo clippy --all-targets --all-features -D warnings`, and `cargo fmt --all --check` — expect all clean. Confirm any new limit is a named units-last constant in `tmx-schema::limits`. Run the `cargo tree` purity check (e.g. `cargo tree -p tmx-core -i tokio` finds nothing) confirming preflight adds no impure edge into `tmx-core`.
  - *Status:* ☒ SATISFIED — independently re-run 2026-07-06 (post-fix): `cargo fmt --all --check` exit 0; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo nextest run` 166/166 passed, 0 skipped (162 prior + the 4 new reference-resolution tests); `scripts/purity.sh` green ("tmx-schema, tmx-core, tmx-testkit carry no I/O or async dependency edge") — the tests drive the async ports with a no-op waker, no runtime linked. No new numeric bound introduced: every limit check names an existing `tmx-schema::limits` constant (TASKS_PER_FLOW_MAX, FANOUT_WIDTH_MAX, JSON_DEPTH_MAX, CONCURRENCY_MAX, HOOK_TASKS_MAX). New helpers carry meaningful assertions (`natural_cmp` total-order + leading-zero cases, `json_depth_exceeds` boundary at 4-vs-3, classify dispatch incl. the `unknown_artifact_kind` negative).

- **O4 — Preflight a directory of task files and confirm natural-filename ordering, and run the fail-fast tests for a malformed task and a missing capability (Reviewable).**
  - *Claim:* A reviewer can preflight a directory and observe tasks in natural-filename order with `environment.*`/`context.*` folded into shared context, and run the two fail-fast tests observing abort / typed errors.
  - *Evidence to collect:* Preflight `docs/examples/folder-layout/` and observe `task-1` before `task-2` with the sibling `environment.*`/`context.*` folded into shared context; run the malformed-task and missing-capability tests via `cargo nextest run` and observe fail-fast.
  - *Status:* ☒ SATISFIED — exercised, not just read: `cargo nextest run -E 'binary(preflight)'` ran all 15 preflight tests, 15 passed. Over the REAL `FileSourceLoader`/`FileReferenceResolver`/`JsonSchemaValidator`: `folder-layout/` yields `["fetch-config", "write-report"]` (task-1 before task-2) with `environment.toml`/`context.yaml` folded in and Http/File/Secret/Process required; the temp-dir malformed task (`type: not-a-real-task-type`) aborts `Validation`/`schema_invalid`; the unwired-fetch run is `Environment`/`missing_capability` naming `HttpClient` and `fetch`. Residue spot-checks done: `task-2` < `task-10` proven end-to-end with a constructed fixture, `task-02` > `task-2` (leading-zero tie-break) and mixed-case byte order in unit tests.

## Regression check

- Preflight composes the Task-13 loader/resolver and Task-14 validator: trace that a valid corpus Flow still preflights — run the preflight over `docs/examples/single-file-flow.yaml` and the `docs/examples/folder-layout/` directory and confirm each still yields a `ResolvedFlow` after the loader/validator are composed behind it.

## Residue

Natural ordering is byte-wise ASCII, case-sensitive, with maximal digit runs compared as unsigned integers — spot-check a mixed-case and a leading-zero case; the corpus `folder-layout/` has only `task-1`/`task-2`, so the `task-2` < `task-10` semantic may need a constructed fixture. The newer-spec compatibility warning is emitted (non-fatal); an unknown construct is fatal `Validation`. Directory context assembly is same-folder only.

## Conclusion

Regression check: run — `single-file-flow.yaml` still preflights over the real loader/validator to a named `ResolvedFlow` (`build-and-publish`, ≥5 tasks, Provider/Store/Chat required) and `folder-layout/` assembles (both PASS); the full suite (166 tests, incl. all task-13/14 loader/resolver/validator tests) is green, so the composed units keep passing. Note: the corpus copy of `single-file-flow.yaml` carries an illustrative `error: ./hooks/on-error.yaml` hook reference whose target does not exist in the repo; now that hook references are (correctly, per 03 §Responsibilities #3) chased, preflighting it *in place* fails typed `reference_not_found` — the regression test stages the example plus a real `hooks/on-error.yaml` in a temp dir, which exercises hook-reference resolution end-to-end over the real `FileReferenceResolver`. The prior in-place pass was a false pass (the dangling reference was silently skipped); a docs follow-up should add `docs/examples/hooks/on-error.yaml` so the flagship example runs out of the box.

VERDICT: ☒ DONE
CONFIDENCE: ☒ high
SUMMARY: ☒ All four obligations discharged. The fix session closed O1's reference gap: `resolve_references` in `crates/tmx-core/src/preflight.rs` chases `environment`/`context` string references and `Hook::Reference`/`Hook::Use` bodies through the `ReferenceResolver`/`SourceLoader` ports (kind-dispatch + JSON-depth bound + schema validation per target, `reference_kind_mismatch` on a contradicting `kind`), inlining everything before `resolve()` — verified positive and negative over fakes (4 new core tests) and over the REAL adapters (validator probes: string env/context refs resolve and their capabilities land in the set; a dangling env or hook reference is typed `Resolution`/`reference_not_found`; a referenced hook body's tasks count against `HOOK_TASKS_MAX` and pull their ports into the capability set). All four cargo gates plus the purity gate green (fmt clean, clippy -D warnings clean, nextest 166/166, purity script green). Residue (non-blocking, out of task-15 scope): provider method-body recursion awaits the task-25 registry; `Hook::Use` `inputs` bindings are dropped when the body is inlined to `Hook::Tasks` (hook-input binding semantics are unimplemented everywhere — the runner passes the outer run's inputs to every hook body); the corpus needs `docs/examples/hooks/on-error.yaml` added (docs change, was out of the implementer's allowed scope).
